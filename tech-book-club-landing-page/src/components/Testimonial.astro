---
// Import any needed images
import iconStar from '../assets/images/icon-star.svg';
---

<section class="py-16 md:py-24 overflow-hidden">
  <div class="container mx-auto px-4 md:px-6">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Star Rating -->
      <div class="flex justify-center mb-6">
        {Array(5).fill(0).map(() => (
          <img src={iconStar.src} alt="" class="w-6 h-6 mx-1" aria-hidden="true" />
        ))}
      </div>

      <!-- Testimonial Quote -->
      <blockquote>
        <p class="font-['Inter'] text-xl md:text-2xl leading-[1.4] text-[#062630] mb-8 relative">
          <span class="absolute -left-8 -top-6 text-6xl text-[#FEA38F] opacity-30">"</span>
          This book club transformed my technical reading from a solitary activity into an enriching
          community experience. The discussions are gold and I've made valuable connections with
          developers who share my interests!
          <span class="absolute -right-8 bottom-0 text-6xl text-[#FEA38F] opacity-30">"</span>
        </p>

        <!-- Attribution -->
        <footer class="mt-6">
          <cite class="not-italic">
            <span class="font-['Martian_Mono'] font-semibold text-lg block text-[#062630]">Sarah Chen</span>
            <span class="font-['Inter'] text-[#385159] text-sm">Software Architect @ TechCorp</span>
          </cite>
        </footer>
      </blockquote>
    </div>
  </div>
</section>